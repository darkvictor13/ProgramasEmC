//como programar em c

//1 passo incluir biblioteca
#include <stdio.h>
#include <stdlib.h>
#include <math.h>
//2 passo por onde inicia a execucao
  int main () {}
  // 3 passo declaracao de variaveis
  int x, raio do circulo ; float y; double a; char c;
  // 4 passo entrada de dados
  scanf ("%formato(d,s,f,lf,x,u,o,c)",&nome_variavel);
  //5 passo processamento mais calculos /(angulos em graus)/log=ln/abs=modulo/floor=arredondar p baixo / ciel=p cima
  float sin(float_x);cos(x);exp(x);asin(x);acos(x);log(x);log10(x);sqrt(x);pow(x,y);floor(x);ciel(x);round(x);
  //6 passo saida de dados
  printf ("qualquer coisa q vc queira na tela = %dado_q_vc_quiser\n",dado_q_vc_quiser);
  //7 passo dar retorno
  return 0;



  //como fazer uma funcao!
  tipo_de_dado_de_retorno [nome_da_funcao] (tipo parametro1,tipo parametro2){
  (calculos da funcao);
  return (oq eu quero q a funcao retorne);}
  //com a funcao pronta basta chamar ela no int main e fazer oq quiser com esse retorno por exemplo
  int main () {
    int x, y;
    scanf("%x",&x);
    y = nome_da_funcao + (/*variaveis_q_eu declarei_no_main_e_quero_jogar_na_funcao,nesse caso x e y)*/(x,y);
  }
  /* a funcao pode nao ter retorno desse modo nos a chamamos de void
    exemplo de funcao void (os jeitos dessa funcao eviar algo p outro lugar e atraves de ponteiro e printf)
    void [nome_da_funcao] (parametros) {
    calculos diversos
  }
      essa funcao nao possue return pq ela e void */


/*ponteiro ele pode apontar para um local de memoria 




*/
